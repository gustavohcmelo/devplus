import React, { Component } from 'react';

class Grupos extends Component {
    render() {
        return (
            <section>
                <h1>Grupos</h1>
            </section>
        );
    }
}

export default Grupos;